[pytest]
# Exclude test files in scripts folder - they cause import errors
# Tests should only be discovered in the tests/ directory
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short

# Ignore scripts folder entirely
norecursedirs = scripts __pycache__ .git .venv venv
